---
apiVersion: v1
kind: Service
metadata:
  name: nginx-static-lb
  namespace: playground
  labels:
    app: nginx-static
spec:
  type: LoadBalancer
  selector:
    app: nginx-static
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
